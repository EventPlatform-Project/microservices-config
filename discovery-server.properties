# discovery-server does not fetch its own service URL from itself, so this file
# stays minimal. Kept here for completeness; discovery-server can also be run
# without config-server.
server.port=8761
eureka.instance.hostname=localhost
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
